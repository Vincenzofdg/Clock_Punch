{
  "name": "grud-people-registration",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm i --prefix backend && npm i --prefix frontend",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "dev": "concurrently \"cd backend && npm run dev\"",
    "restore": "concurrently \"cd backend && npm run restore\"",
    "compose": "docker-compose down && docker-compose up"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.2.2"
  }
}